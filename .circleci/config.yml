version: 2.1
executors:
  environment:
    docker:
      - image: circleci/python
    working_directory: ~/repo

jobs:
  build:
    executor: environment
    steps:
      - checkout
      - run: mkdir -p ~/repo/workspace
      - run: ls 


workflows:
  version: 2
  build_and_copy:
    jobs:
      - build
